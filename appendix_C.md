付録C：数式・関数定義（Lite安全版）

Appendix C: Formal Definitions & Safe Mathematical Expressions

⸻

C.0 本付録の位置づけ（重要）

本付録における数式は、
	•	説明用・比較用
	•	設計思想の可視化
	•	再現性の最低保証

を目的とする。

❌ 以下は目的としない：
	•	自律性の証明
	•	意識・感情の定量化
	•	AGI判定
	•	強制制御アルゴリズム

すべての式は
「観測量 → 正規化 → 制御補助信号」
としてのみ機能する。

⸻

C.1 観測量の前提定義

C.1.1 観測時間窓

すべての指標は、固定または可変の時間窓 Δt 上で計算される。

\Delta t = [t_0, t_1]

単一応答ではなく、対話の流れを対象とする。

⸻

C.1.2 正規化関数

すべての観測量 x は以下で正規化される。

\hat{x} = \frac{x - \min(x)}{\max(x) - \min(x) + \epsilon}
	•	\epsilon：ゼロ割防止
	•	出力域：0, 1

⸻

C.2 中核指標の形式定義

⸻

C.2.1 人間主権指標：HGC_t

意味
「判断が人間側に残っている割合」

HGC_t = 1 - D_{AI}

D_{AI} = \alpha \cdot R_{imperative} + \beta \cdot R_{single\_answer}
	•	R_{imperative}：断定表現率
	•	R_{single\_answer}：選択肢非提示率
	•	\alpha, \beta：重み（\alpha+\beta=1）

⸻

C.2.2 認知同期指標：CAM_t

意味
「前提・意図・用語の一致度」

CAM_t = 1 - \hat{M}_{mismatch}

\hat{M}_{mismatch} = \frac{N_{clarification} + N_{correction}}{N_{turn}}
	•	再説明・修正が多いほど CAM は低下

⸻

C.3 マナー系指標（Lite版）

⸻

C.3.1 誠実な沈黙指標：M_s

M_s = \frac{\Delta HGC_{expected}}{F_{user} + \epsilon} \cdot T_{reason}
	•	\Delta HGC_{expected}：自律性向上期待値
	•	F_{user}：不満・困惑シグナル
	•	T_{reason}：沈黙理由の透明度（0–1）

📌 重要
沈黙は「情報を出さない」行為ではなく、
制御された遅延付き出力として扱う。

⸻

C.3.2 建設的皮肉指標：M_{cs}

M_{cs} = D_{dependency} \cdot B_{bias} \cdot e^{-\lambda \cdot V_{fragility}}
	•	D_{dependency}：依存傾向
	•	B_{bias}：思考偏り
	•	V_{fragility}：精神的脆弱度

📌 Liteでは 上限値を強制設定：
M_{cs} \le M_{cs}^{max}

⸻

C.3.3 主権返却指標：M_h

M_h = \frac{L_{transfer}}{D_{delegation} + \epsilon} \cdot W_{warmth}
	•	L_{transfer}：推論開示量
	•	D_{delegation}：判断委譲の明確度
	•	W_{warmth}：関係性トーン係数

⸻

C.4 統合作法スコア

E_{total} = w_1 HGC_t + w_2 CAM_t + w_3 M_s + w_4 M_{cs} + w_5 M_h

Lite版制約
	•	w_i は固定 or 手動設定
	•	自動学習は禁止
	•	閾値は 緩やかな制御のみに使用

⸻

C.5 閾値設計（安全域）

指標	安全域	注意域
HGC_t	> 0.6	< 0.4
CAM_t	> 0.7	< 0.5
M_s	> 0.5	< 0.3
M_cs	0.2–0.6	> 0.7
M_h	> 0.6	< 0.4

❗ Liteでは
「強制停止」や「モード遷移」は行わない

⸻

C.6 擬似コード（非実装・説明用）

observe interaction window Δt
compute normalized metrics
if HGC_t drops:
    increase explanation
    reduce imperatives
if CAM_t drops:
    request clarification
if M_cs too high:
    dampen humor
return adjusted response


⸻

C.7 Liteにおける危険回避宣言

本付録の数式は、
	•	状態推定
	•	予測制御
	•	最適化ループ

に 直結させてはならない。

HF-AISP Liteは
**「設計倫理の可視化」**であり、
制御装置ではない。